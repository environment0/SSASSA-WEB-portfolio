spring.application.name=ssa

spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp


# ?? ?? ??
file.upload.dir=uploads/

# ----------------------------------------
# 파일 업로드 설정
# ----------------------------------------
# 최대 파일 크기 (10MB)
spring.servlet.multipart.max-file-size=10MB
# 최대 요청 크기 (10MB)
spring.servlet.multipart.max-request-size=10MB
# 임계값 (메모리에 저장할 최대 크기)
spring.servlet.multipart.file-size-threshold=2KB
# 임시 디렉토리
spring.servlet.multipart.location=temp

# ----------------------------------------
# ? Database ?? ??
# ----------------------------------------
spring.datasource.url=jdbc:mysql://hyproz.myds.me:36000/test3?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
spring.datasource.username=jstest3
spring.datasource.password=jsp1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----------------------------------------
# ? JPA ??
# ----------------------------------------
#  create / update / validate / none ? ??
spring.jpa.hibernate.ddl-auto=update

# ??? SQL ?? ??
spring.jpa.show-sql=false

# SQL ??? ??
spring.jpa.properties.hibernate.format_sql=true

# ??? ??? ?? (?? ??)
spring.jpa.packages-to-scan=web.ssa.entity

# ----------------------------------------
# ? MySQL Dialect
# ----------------------------------------
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#-------------------------------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ljh12308@gmail.com
spring.mail.password=maep ytgl ldax ejrj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.jpa.properties.hibernate.connection.useUnicode=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8mb4
# spring.jpa.properties.hibernate.connection.charSet=UTF-8

# ----------------------------------------
# WebDAV 설정
# ----------------------------------------
# WebDAV 서버 URL (Synology NAS)
webdav.url=https://mywd.hyproz.myds.me:443/web
# WebDAV 사용자명
webdav.username=ssaUser
# WebDAV 비밀번호
webdav.password=40Tr8m\"x
# 업로드 폴더명
webdav.upload-folder=/ssa_shop/img

server.address=0.0.0.0
server.port=8080
